# Секреты
.env
.env.local

# Локальные артефакты прогонов
node_modules/
.promptfoo/
output/
*.log
_promptfoo_log_*.txt

# Скрытые тест-сеты (с эталонами) — НЕ коммитим, чтобы кандидаты не увидели.
# Каркас (yaml + структура) — коммитим. Конкретные кейсы хранятся локально.
tasks/*/hidden/

# Локальные сдачи кандидатов
submissions/
